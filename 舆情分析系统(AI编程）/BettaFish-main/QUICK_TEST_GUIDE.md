# BettaFish Bug 修复快速测试指南

**修复日期**: 2026-04-16  
**修复内容**: 前端报告展示阶段的三个严重 Bug  
**预计测试时间**: 10-15 分钟

---

## 🚀 快速启动

### 1. 启动数据库（如果未运行）
```bash
docker ps | grep bettafish-db
# 如果没有运行，执行：
docker start bettafish-db
```

### 2. 启动后端
```bash
cd "c:\Users\59849\Desktop\舆情分析系统(AI编程） (1)\舆情分析系统(AI编程）\BettaFish-main"
python app.py
```

**预期输出**：
```
 * Running on http://0.0.0.0:8080
 * Running on http://127.0.0.1:8080
```

### 3. 打开浏览器
访问: `http://localhost:8080`

---

## ✅ 测试清单

### 测试 1: 新搜索流程（验证 Bug 1 + Bug 2）

**步骤**：
1. 在搜索框输入关键词（如"人工智能"）
2. 点击【开始分析】
3. 按 `F12` 打开开发者工具
4. 切换到 `Network` 标签

**观察点 1 - 三路引擎运行**：
- ✅ 应该看到三个绿色 Agent 节点（全网实时资讯、权威深度报道、内部核心智库）
- ✅ 节点应该从"待命"→"运行中"→"✅ 完成"
- ✅ 控制台应该输出实时日志

**观察点 2 - 报告生成完成**：
- ✅ Network 标签中应该看到 `GET /api/report/result/[短hash]` 返回 `200`
- ✅ **不应该**出现 `404` 错误
- ✅ **不应该**出现 `report-xxxxx` 格式的长 ID

**观察点 3 - UI 清理效果**：
报告加载完成后，页面应该：
- ✅ 三个绿色 Agent 节点**完全消失**
- ✅ 控制台输出区域**完全消失**
- ✅ 全息进度条蒙版**平滑淡出并移除**
- ✅ 报告内容**清晰可见**，没有任何遮挡
- ✅ 页面只剩下：星空背景 + 报告卡片 + 浮动工具栏

**如果出现问题**：
- ❌ 如果看到 404 错误 → Bug 1 未修复
- ❌ 如果绿色节点残留 → Bug 2 未修复
- ❌ 如果蒙版残留 → Bug 2 未修复

---

### 测试 2: 历史报告查看（验证 Bug 3）

**步骤**：
1. 点击页面右上角的【历史报告】按钮
2. 在弹出的抽屉中选择刚才生成的报告
3. 观察控制台（F12 → Console 标签）

**观察点 1 - 无错误**：
- ✅ **不应该**出现 `Uncaught ReferenceError: _holoPolling is not defined`
- ✅ **不应该**出现任何 JavaScript 错误

**观察点 2 - 报告正常加载**：
- ✅ 报告应该正常显示
- ✅ UI 应该干净，没有残留蒙版
- ✅ 三个绿色 Agent 节点应该完全消失

**如果出现问题**：
- ❌ 如果出现 `_holoPolling is not defined` → Bug 3 未修复
- ❌ 如果报告无法加载 → 检查后端日志

---

### 测试 3: 边界情况测试（可选）

#### 3.1 快速切换历史报告
**步骤**：
1. 打开【历史报告】
2. 快速点击多个不同的报告（间隔 < 1 秒）

**预期**：
- ✅ 不应该出现 UI 残留
- ✅ 不应该出现 JavaScript 错误
- ✅ 最后点击的报告应该正常显示

#### 3.2 刷新页面测试
**步骤**：
1. 在报告加载过程中（全息进度条显示时）
2. 按 `F5` 刷新页面

**预期**：
- ✅ 页面应该正常重新加载
- ✅ 不应该出现异常状态

#### 3.3 网络错误测试
**步骤**：
1. 打开开发者工具 → Network 标签
2. 勾选 `Offline`（模拟断网）
3. 点击【历史报告】并选择一个报告

**预期**：
- ✅ 应该显示友好的错误提示
- ✅ 不应该出现未捕获的异常

---

## 📊 测试结果记录

### Bug 1: Task ID 错配
- [ ] ✅ 通过 - Network 请求使用短 hash，返回 200
- [ ] ❌ 失败 - 出现 404 错误或使用长 ID

**失败时的截图/日志**：
```
（粘贴 Network 标签截图或错误日志）
```

---

### Bug 2: DOM 骨架丢失与蒙版残留
- [ ] ✅ 通过 - Agent 节点完全消失
- [ ] ✅ 通过 - 控制台完全消失
- [ ] ✅ 通过 - 全息进度条平滑移除
- [ ] ✅ 通过 - 报告内容清晰可见
- [ ] ❌ 失败 - 有残留元素

**失败时的截图**：
```
（粘贴页面截图，标注残留元素）
```

---

### Bug 3: _holoPolling 未定义
- [ ] ✅ 通过 - 历史报告正常打开，无错误
- [ ] ❌ 失败 - 出现 `_holoPolling is not defined` 错误

**失败时的控制台日志**：
```
（粘贴 Console 标签的错误日志）
```

---

## 🐛 如果测试失败

### 1. 检查代码是否正确应用
```bash
cd "c:\Users\59849\Desktop\舆情分析系统(AI编程） (1)\舆情分析系统(AI编程）\BettaFish-main"
python test_bugfix.py
```

**预期输出**：
```
🎉 所有 Bug 修复验证通过！
```

### 2. 清除浏览器缓存
1. 按 `Ctrl + Shift + Delete`
2. 勾选"缓存的图片和文件"
3. 点击"清除数据"
4. 刷新页面（`Ctrl + F5` 强制刷新）

### 3. 检查后端日志
查看后端终端输出，寻找以下关键信息：
- `[桥接]` - 报告查询相关
- `[ReportEngine]` - 报告生成相关
- `ERROR` - 错误信息

### 4. 检查数据库
```bash
docker exec -it bettafish-db psql -U bettafish -d bettafish_db -c "SELECT task_id, report_task_id, query_raw, created_at FROM report_cache ORDER BY created_at DESC LIMIT 5;"
```

**预期输出**：应该看到最近生成的报告记录，`task_id` 字段应该是短 hash 格式。

---

## 📝 测试完成后

### 如果所有测试通过
恭喜！三个 Bug 已全部修复。你可以：
1. 正常使用系统进行舆情分析
2. 查看历史报告
3. 导出报告（PDF/HTML/Markdown）

### 如果部分测试失败
请提供以下信息以便进一步排查：
1. **失败的测试项**（Bug 1/2/3）
2. **浏览器控制台截图**（F12 → Console 标签）
3. **Network 请求截图**（F12 → Network 标签）
4. **后端终端日志**（最后 50 行）
5. **具体的复现步骤**

---

## 🎯 性能基准

修复后的系统性能应该与修复前一致：

| 指标 | 预期值 |
|------|--------|
| 搜索启动时间 | < 1 秒 |
| 三路引擎总耗时 | 30-120 秒（取决于关键词） |
| 报告加载时间 | < 3 秒 |
| 历史报告打开时间 | < 2 秒 |
| UI 清理时间 | < 0.6 秒（平滑淡出） |

---

## 📚 相关文档

- **详细修复报告**: `BUGFIX_REPORT.md`
- **系统架构**: `memory/project_architecture.md`
- **启动流程**: `memory/project_startup.md`
- **强制约束**: `CLAUDE.md`

---

**测试人员**: _______________  
**测试日期**: _______________  
**测试结果**: [ ] 全部通过 [ ] 部分失败 [ ] 全部失败
